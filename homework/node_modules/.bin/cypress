#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/../.store/cypress@13.15.1/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/../.store/cypress@13.15.1/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../.store/cypress@13.15.1/node_modules/cypress/bin/cypress" "$@"
else
  exec node  "$basedir/../.store/cypress@13.15.1/node_modules/cypress/bin/cypress" "$@"
fi
